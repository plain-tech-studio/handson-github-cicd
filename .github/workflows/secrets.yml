name: Secrets
on: push
jobs:
  print:
    runs-on: ubuntu-latest
    env:
      PASSWORD: ${{secrets.PASSWORD}}
    steps:
      - run: echo "${PASSWORD}"                   # ログ出力はマスクされる
      - run: echo "${PASSWORD:0:1} ${PASSWORD#?}" # 「一文字目 + スペース + 二文字目以降」マスクされない